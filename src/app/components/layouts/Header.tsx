import React from "react";

const Header = () => {
  return (
    <header className="flex items-center justify-between p-4 bg-blue-600 text-white shadow-md">
      <h1 className="text-lg font-semibold">Flood Monitoring Dashboard</h1>
    </header>
  );
};

export default Header;
